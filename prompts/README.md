# Prompts

Edit these files to change LLM behavior without touching code.

## triage_system.txt

System prompt for triage. The literal `{metrics}` is replaced with case-specific patient data (adherence, sessions, FM scores, etc.) on each call.
